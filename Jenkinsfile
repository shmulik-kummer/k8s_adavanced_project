pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/avielb/rmqp-example'
            }
        }

        // Add more stages for subsequent steps as needed
    }
}
